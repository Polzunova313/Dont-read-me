Если не удаётся подключиться через пару ключей SSH необходимо установить Git hub CLI и подключаться через HTTPS.

Это изменение на локальном компьютере

<code>git add .\
git commit -m 'local'\
    *enter*\
git push</code>

Изменение 2. 88005553535
